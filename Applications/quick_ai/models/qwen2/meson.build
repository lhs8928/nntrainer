qwen2_src = [
    meson.current_source_dir() / 'qwen2_causallm.cpp',
    meson.current_source_dir() / 'qwen2_embedding.cpp',
]

qwen2_inc = include_directories('.')

quick_ai_inc += qwen2_inc
quick_ai_src += qwen2_src
