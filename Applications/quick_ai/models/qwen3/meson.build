qwen3_src = [
    meson.current_source_dir() / 'qwen3_causallm.cpp',
    meson.current_source_dir() / 'qwen3_embedding.cpp',
]

qwen3_inc = include_directories('.')

quick_ai_src += qwen3_src
quick_ai_inc += qwen3_inc
